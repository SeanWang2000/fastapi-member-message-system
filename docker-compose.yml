services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    environment:
      DB_HOST: host.docker.internal

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
